from setuptools import setup, find_packages

setup(
    name="hyperwave-mock",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.7",
    description="Mock package for hyperwave-community",
    author="Your Name",
    install_requires=[],
)